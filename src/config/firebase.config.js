/**
 * Firebase Configuration Stub
 *
 * Firebase Cloud Messaging has been removed from this application.
 * This file exports a null messaging object for backward compatibility.
 *
 * Push notifications are now handled by a stub implementation in
 * notification.service.js that logs notifications to the console instead
 * of sending them via FCM.
 *
 * All notification data is still saved to the database as before.
 */

// Export null messaging for backward compatibility
const messaging = null;

module.exports = { messaging };
